# tuples cannot change their value
# it is a immutable data type in python (can't change)
# t = (1, 3, 4, 5) tuple with more than one element
# print(t[2])
# t[2] = 43 it doesn't change its value
# t1 = () # empty tuple
t1 = (2,) #Tuple with single element
print(t1)